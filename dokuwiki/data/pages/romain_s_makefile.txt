<code make>
GZ := $(wildcard *.seq.gz)
UNZIPPED := $(GZ:.seq.gz=.seq)
DIR = unzipped

all: $(DIR) $(UNZIPPED)

%.seq: %.seq.gz
	gunzip -c $< > $(DIR)/$@

$(DIR):
	mkdir $(DIR)

clean:
	rm $(DIR)/*
	rmdir $(DIR)

</code>

[[start]]